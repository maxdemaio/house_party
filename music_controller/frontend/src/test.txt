// Lifecycle method call server endpoint
  // We use async so we can use an aync operation inside our function 
  // We don't need to wait on this to finish essentially
  // async componentDidMount() {
  //   fetch("api/user-in-room")
  //   .then((response) => {response.json()})
  //   .then((data) => {
  //     this.setState({
  //       // Grab roomcode from API endpoint
  //       // If the user was already in a room we can map them to it
  //       roomCode: data.code
  //     })
  //   });
  // }

  // renderHomePage() {
  //   return (
  //     <Grid container spacing={3} align="center">
  //       <Grid item xs={12}>
  //         <Typography variant="h3" compact="h3">
  //           House Party
  //         </Typography>
  //       </Grid>
  //       <Grid item xs={12} align="center">
  //         <ButtonGroup disableElevation variant="contained" color="primary">
  //           <Button color="primary" to="/join" component={Link}>
  //             Join a Room
  //           </Button>
  //           <Button color="secondary" to="/create" component={Link}>
  //             Create a Room
  //           </Button>
  //         </ButtonGroup>
  //       </Grid>
  //     </Grid>
  //   );
  // }